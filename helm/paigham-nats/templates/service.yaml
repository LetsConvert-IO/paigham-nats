apiVersion: v1
kind: Service
metadata:
  name: {{ include "paigham-nats.fullname" . }}
  labels:
    {{- include "paigham-nats.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paigham-nats.selectorLabels" . | nindent 4 }}
